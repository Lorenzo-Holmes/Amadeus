STATUS=DRAFT_PROPOSAL
IMPLEMENTATION_AUTHORITY=NONE
SOL_GATE=PENDING

# M5.2a3 ViewBuilder 契约闭合提案

[INFERRED] 反方论据：若先写入 `ViewBuilder`，再补齐返回基数、命令边界、派生存储和审计语义，五类视图会把非权威数据误当作第四权威层，并使失败回退与跨 Vault 隔离不可验证。本提案只选择可测试的契约，不实施任何运行时代码。
CONFIDENCE: HIGH

[FRAME] 本文是供 Sol 审批的非权威提案；它不修改 Constitution、现有冻结接口、数据库、注册表、夹具或测试，也不授予任何后续包执行权限。
CONFIDENCE: HIGH

## Current state

[KNOWN] 当前冻结基线声明 `ViewBuilder.rebuild(command: MutationCommandEnvelope, vault_id: str, branch_id: str) -> CommandResult[MaterializedViewManifest]`，而 `MaterializedViewManifest.view_type` 允许 `summary`、`timeline`、`vector`、`fulltext`、`cue` 五类值。该单数返回和五类产物之间尚未有已批准的基数契约。
CONFIDENCE: HIGH

[KNOWN] `MaterializedViewManifest`、`RetrievalRequest` 和 `ExpressionDecision` 均不在 `AUTHORITATIVE_MODELS` 或 `TYPE_REGISTRY` 中；现有合约测试已断言物化视图不可经权威写入边界保存。
CONFIDENCE: HIGH

[KNOWN] 现有 `AuthorityRepository.validated_vault_candidates` 已对 Memory 和 Ledger 执行 identity、lineage、branch、vault 的精确过滤；`SourceSnapshot` 当前没有 Vault 字段。
CONFIDENCE: HIGH

[KNOWN] 已审阅的 AC-018 夹具要求对 Vault A 缓存键与 Vault B 请求在缓存访问前以 `CORE-E-VAULT-SCOPE-MISMATCH` 拒绝，并保持缓存状态不变。
CONFIDENCE: HIGH

[KNOWN] AC-031 与 AC-032 的 Stage0C reviewed fixtures 当前缺失；Stage0B 报告仍将 `case_coverage_complete` 和 `catalog_ready` 记为 false。
CONFIDENCE: HIGH

[KNOWN] 本包的冻结输入散列与 TaskPacket 所列四个值一致：`views.py=0B4E69ED415EAFEE9E79BE72AFBF8A293402B2DBE67D282085059EB93B09ACC2`、`retrieval/service.py=DBC713F12D1174BEA12F57C204F93B6A4549440F5FD33E5F0B8DF2D933205182`、`repository.py=6DBFC9AFCF8B82B860A4A97D8354D07D34FCC802BC6059B16E78E4A0DD4B92F5`、`unit_of_work.py=3A7DA8B32CBA03402063544E430D195A4A2D1100A0DBE31D42748BD035457D0A`。
CONFIDENCE: HIGH

## Recommended specification

[FRAME] 下列九项各自只给出一个推荐结论。所有 `MUST` 仅在 Sol 批准本提案及相应后续 TaskPacket 后生效。
CONFIDENCE: HIGH

### D01_RESULT_CARDINALITY

| field | contents |
|---|---|
| evidence | [KNOWN] `MaterializedViewManifest` 当前一次只描述一种 `view_type`，并含 identity/branch/vault 但不含 lineage；冻结基线返回单数，而目标集合为五类。 CONFIDENCE: HIGH |
| recommended resolution | [FRAME] 一次成功的 `rebuild` 必须原子地产生恰好五个 manifest，顺序固定为 `("summary", "timeline", "vector", "fulltext", "cue")`，且每种恰好一个；单数 `CommandResult` 承载 `RebuiltMaterializedViews`。`RebuiltMaterializedViews` 与 `DerivedViewFallback` 都必须具有非权威 scope fields `identity_id`、`lineage_id`、`branch_id`、`vault_id`。`RebuiltMaterializedViews` 的 Pydantic runtime validator 只直接校验：`manifests` 长度为 5、view_type 唯一且固定顺序、每项 manifest 的 identity/branch/vault 与聚合 scope 一致、以及五项 watermark/root/builder 一致；不得声称 manifest 自身含 lineage。lineage 必须由 ViewBuilder 在构造时从已验证 command/capability/vault scope 绑定，并通过 D04 source-root preimage、D05 view-hash 重算、P2 store scope/CAS 和 reader 校验闭合。 CONFIDENCE: HIGH |
| rejected alternatives | [INFERRED] 拒绝每次只建一种、任意长度列表、五次独立命令、只在静态类型声明五元组、或在 validator 读取不存在的 manifest lineage；这些方案分别不能表达全量完成、缺少完备性、不能原子观察、会接受不一致反序列化，或形成不可达校验。 CONFIDENCE: HIGH |
| interface impact | [FRAME] 需后续 Sol 批准：`CommandResult[MaterializedViewManifest]` 变更为 `CommandResult[RebuiltMaterializedViews | DerivedViewFallback]`；两个聚合结果新增 I/L/B/V scope 字段。原 `MaterializedViewManifest` 字段不变，也不进入权威模型注册表。 CONFIDENCE: HIGH |
| tests after approval | [FRAME] 添加五类齐全、重复/缺少/乱序、manifest identity/branch/vault 与聚合不一致、watermark/root/builder 不一致、伪造 aggregate lineage 后 D04 root 重算拒绝、单个视图构建失败回滚和一次重建只生成一个 receipt 的测试。 CONFIDENCE: HIGH |
### D02_COMMAND_CONTRACT

| field | contents |
|---|---|
| evidence | [KNOWN] `MutationCommandEnvelope` 已冻结 command、actor、capability、expected versions、idempotency、target refs 和闭合 payload；MaintenanceCapability 已有 attestation、approval refs、evidence seal、operation/resource 与时间字段，但当前没有本提案规定的维护验证协议或无 receipt abort。 CONFIDENCE: HIGH |
| recommended resolution | [FRAME] 唯一命令为 `command_type="maintenance.rebuild_index"`，actor 仅 `maintainer`。payload 键集合必须精确等于 `{ "operation", "instance_id", "scope_refs", "builder_version", "rebuild_event_id", "validation_failed_event_id", "fallback_event_id", "expected_derived_generation", "expected_derived_state_hash" }`；`operation="rebuild_index"`，scope 按 `(I,L,B,V)`，builder 为 `view-builder-v1`，三枚预分配事件 ID 两两不同，generation 为非负整数，state hash 为小写 SHA-256。target refs 按 `(actor_capability_id,R,F,G)`，expected versions 同序为 capability 当前版本及三个 `absent`；它只是允许权威写入的超集：成功只 append R，fallback 只 append F/G，未使用 ID 保持 absent，`CommandResult.event_ids` 只列实际 append 事件。P3 在 `view_builder.py` 定义内部协议 `MaintenanceAttestationVerifier.verify(attestation: str, payload_hash: str) -> bool` 与 `MaintenanceApprovalRegistry.is_approved(approval_refs, evidence_seal_ref, identity_id, lineage_id, branch_id, exact_operation, exact_resource_ref) -> bool`；两个调用的返回值都必须 `is True`，任何 `False`、`None`、`1`、字符串或 truthy object 一律 fail closed。attestation payload hash 为 SHA-256(canonical JSON) 且只含 `{capability_id,maintainer_id,identity_id,lineage_id,branch_id,reason_code,exact_operation,exact_resource_ref,not_before,expires_at,approval_refs,evidence_seal_ref,use_limit}`，明确排除 `record_header`、`attestation`、`used_at`、`status`、`version`。handler 先 `get_validated(actor_capability_id)`，校验 status=`issued`、`not_before<=now<expires_at`、`used_at=None`、`use_limit=1`、maintainer actor、I/L/B、operation/resource、两协议、approval/evidence seal；再验证 RelationshipVault 的 I/L/B/V 与 allowed status。时间失效映射 `MAINTENANCE_CAPABILITY_EXPIRED`，已使用映射 `MAINTENANCE_CAPABILITY_CONSUMED`，其余 capability/binding/attestation/approval/Vault 拒绝映射 `MAINTENANCE_SCOPE_EXCEEDED`。全部前置拒绝抛下述无 receipt abort。通过前置校验后，handler 在同一 serialized transaction 用 P2 guard CAS 比较 generation/state hash；不相等返回 `STALE_VERSION` 且零 derived/Ledger/capability/receipt mutation。仅 success/fallback 后同事务 consume capability、写实际事件和 receipt；guard 不伪装为 UoW authority target，也不改变 UoW target-version 语义。 CONFIDENCE: HIGH |
| rejected alternatives | [INFERRED] 拒绝无 capability 的内部调用、宽泛 actor、truthy 审批/attestation 返回、将 mutable capability 字段纳入 attestation payload、自由 payload、把 manifest/guard 当权威 target、成功/F/G 全部 append，或没有 generation CAS 的不同 idempotency key 竞争；这些方案会削弱范围、允许类型混淆、破坏签名稳定性、伪造权威性、污染审计或丢失更新。 CONFIDENCE: HIGH |
| interface impact | [FRAME] 需后续 Sol 批准：`rebuild(command: MutationCommandEnvelope)` 从 payload 解包完整 scope；P3 在 `unit_of_work.py` 暴露受限 `CommandAbortWithoutReceipt`，仅允许携带 `event_ids=()` 的 CommandResult。handler 抛出该异常后 serialized transaction 回滚，`execute_command_on_connection` 在事务外捕获并返回，不写 receipt；异常携带非空 event_ids 必须拒绝。该路径仅用于 capability/command/Vault 前置拒绝和 derived CAS stale；success/fallback 保持 receipt。 CONFIDENCE: HIGH |
| tests after approval | [FRAME] 添加 command/actor/payload 精确键集、attestation preimage/hash、两个协议对 built-in True 以外返回值的 fail-closed、错误映射、capability/Vault 前置拒绝零 receipt、abort 非空 event_ids 滥用拒绝、成功/fallback consume、CAS stale 零写、以及相同/不同 idempotency key 的 branch/vault 竞争测试。 CONFIDENCE: HIGH |
### D03_DERIVED_PERSISTENCE

| field | contents |
|---|---|
| evidence | [KNOWN] 当前 SQLite loader 只处理初始权威迁移，且当前规范没有已批准的派生 store table 数量；现有 UnitOfWork 可把同一连接内的写入和 receipt 原子提交，manifest 不具备权威 RecordHeader。 CONFIDENCE: HIGH |
| recommended resolution | [FRAME] three-table layout 是本提案为首次空 scope、generation/state CAS 与 FK cleanup 提出的明确 schema 差异，requires later Sol approval；它不是现有规范事实，并且三表均为可丢弃、可重建的非权威投影，不形成第四权威层。P2 必须引入 `schema_migrations(version, migration_sha256, applied_at)`：既有 0001 数据库先验证既有 schema fingerprint、登记 0001 hash，再顺序应用 0002；fresh 数据库创建 metadata 后按序应用并登记 0001、0002；latest fingerprint 不匹配必须 drift fail。派生三表精确为：`derived_view_scopes` 以 `(identity_id,lineage_id,branch_id,vault_id)` composite PK 保存 `generation` 与 `semantic_state_hash`；`derived_view_manifests` 以 scope+`view_type` composite PK、`view_id UNIQUE`、FK scope `ON DELETE CASCADE`，并保存 `manifest_json`、`manifest_hash`、`content_hash`；`derived_view_contents` 以 `view_id` PK、FK manifests(view_id) `ON DELETE CASCADE`，保存 canonical `content_json` 与 `content_hash`。store 执行 CHECK、lowercase hex/hash、JSON decode 与 canonical-byte equality 验证。`replace_scope` 在一个 SQLite transaction 中 CAS guard、delete 旧五行、insert manifest/content、更新 generation+semantic state hash，随后同一 transaction 处理 audit、consume 与 receipt；外部 cache/adapter 仅能提交后可丢弃写入。 CONFIDENCE: HIGH |
| rejected alternatives | [INFERRED] 拒绝把派生数据存入 authority records、进程内存、外部 adapter 伪原子双写、无 migration history 的 replacement、无 guard 的 delete/insert，或把 generation/state hash 重复在五条 manifest 行；最后一种使首次空 scope 没有单一 CAS 锚点，并使五行并发一致性更脆弱。三表不是既有规范已批准事实，故也拒绝把它表述为当前实现。 CONFIDENCE: HIGH |
| interface impact | [FRAME] 需后续 Sol 批准：新增内部 `SQLiteDerivedViewStore`、three-table migration 0002 和顺序 migration loader；不向 AuthorityRepository、`AUTHORITATIVE_MODELS` 或 `TYPE_REGISTRY` 添加派生模型。若 Sol 不批准该 layout，P2 必须另起契约差异提案，而不是静默替换。 CONFIDENCE: HIGH |
| tests after approval | [FRAME] 添加 fresh/legacy 迁移顺序、hash 登记、latest fingerprint drift、三表 FK/CHECK、canonical JSON、空 scope 初始 CAS、五行 replace、CAS conflict、insert failure rollback、删除后重建和外部 cache 失败不影响 SQLite 的测试。 CONFIDENCE: HIGH |
### D04_SOURCE_SET_AND_ROOT

| field | contents |
|---|---|
| evidence | [KNOWN] SourceSnapshot 绑定 I/L/B 但无 Vault；LedgerEvent 具有可空 Vault 和有序 ledger sequence；Memory 具有 governing Vault；RelationshipVault 提供真实 scope 绑定与状态。 CONFIDENCE: HIGH |
| recommended resolution | [FRAME] source root 前必须 `get_validated(V)` 并验证 RelationshipVault 的 I/L/B/V 精确一致且 status 属于 `{ "active", "contact_paused" }`；无效 scope 必须 precondition fail，不得生成伪空 root。三层只纳入完整性验证记录：active 且 I/L/B 匹配的 SourceSnapshot 作为每个该 branch Vault 的 global branch-origin metadata，其 descriptor 仅为 `{layer:"source_snapshot",record_id:snapshot_id,record_version:version,integrity_hash:content_hash,payload_root_hash:payload_root_hash}`，不得解析/暴露 `source_ref` 或 payload，也不得称为 Vault-owned content；Ledger 需 I/L/B/V 精确匹配并排除三种派生审计 event；Memory 需 I/L/B/governing_vault 精确匹配。layer 顺序为 snapshot、ledger、memory；层内为 `(snapshot_id,version)`、`(ledger_seq,event_id)`、`(memory_id,semantic_version,version)`。watermark 为纳入 Ledger 最大 seq，无 Ledger 为 0；root preimage 固定含 root version、I/L/B/V、watermark 和上述 sorted descriptors，`source_root_hash=SHA256(canonical_json(preimage))`。空集合只在 scope 已验证而三层均无记录时合法。 CONFIDENCE: HIGH |
| rejected alternatives | [INFERRED] 拒绝不验证 Vault 就生成空 root、把 branch origin 当 Vault 内容、解析 SourceSnapshot source_ref/payload、要求不可达 Vault import event、wall-clock 排序、只纳入 active Memory 或把派生审计 event 重作输入；这些方案会掩盖无效范围、泄漏内容、使 SourceSnapshot 不可达、造成非确定性、遗漏权威状态或产生递归自引用。 CONFIDENCE: HIGH |
| interface impact | [FRAME] 需后续 Sol 批准：新增只读 source-set builder，输入必须是完整 I/L/B/V；不改变任何权威 record model。 CONFIDENCE: HIGH |
| tests after approval | [FRAME] 添加 Vault 无效/密封拒绝而非空 root、branch-origin metadata 纳入且无 source_ref/payload、三层 scope 排除、派生审计排除、稳定排序、空集合和 root golden bytes 的测试。 CONFIDENCE: HIGH |
### D05_BUILDER_AND_VIEW_HASH

| field | contents |
|---|---|
| evidence | [KNOWN] manifest 已有 builder version、watermark、root 与 view hash；项目 canonical JSON/SHA-256 已冻结确定性编码。 CONFIDENCE: HIGH |
| recommended resolution | [FRAME] `VIEW_BUILDER_VERSION="view-builder-v1"` 是唯一 v1 authority，兼容性仅 exact equality。五类均是 opaque deterministic reference indexes，不是自然语言摘要、语义 embedding 或载荷全文；内部 `source_ref` 一律是权威 `record_id`，绝不是 SourceSnapshot.source_ref/payload_ref。设 D 为 D04 descriptors、`descriptor_hash=SHA256(canonical_json(descriptor))`：summary=`{kind:"summary",descriptor_hashes:[D全部hash]}`；timeline=`{kind:"timeline",entries:[{source_ref:event_id,ledger_seq,descriptor_hash}仅Ledger]}`；vector=`{kind:"vector",descriptor_hashes:[D全部hash]}`；fulltext=`{kind:"fulltext",source_refs:[D全部record_id]}`；cue=`{kind:"cue",entries:[{source_ref:record_id,descriptor_hash}全部D]}`。`view_id="viw-"+SHA256(canonical_json({view_id_version:"materialized-view-id-v1",scope_refs:[I,L,B,V],view_type:T}))[:32]`。每类 `view_hash` 为 `{view_hash_version,view_type,I,L,B,V,watermark,source_root_hash,builder_version,content}` 的 canonical SHA-256。derived semantic state projection 排除 `built_at`，仅含固定 scope、view_type、view_id、watermark、root、builder、view_hash、content_hash 的五类 sorted entries；其 hash 是 D02 CAS state hash。 CONFIDENCE: HIGH |
| rejected alternatives | [INFERRED] 拒绝调用方自报 builder version、宽松兼容、外部 encoder/payload resolver、把 source_ref 解释为外部来源、随机 view ID、或把 built_at 纳入 semantic state；它们会破坏确定性、原子性、隔离或同源不同 clock 的 CAS 稳定性。 CONFIDENCE: HIGH |
| interface impact | [FRAME] 需后续 Sol 批准：MaterializedViewManifest 保持非权威；新增结果与 store 采用确定性 view ID/state projection。改变 builder version、content preimage 或 ID version 需要新 Sol 提案。 CONFIDENCE: HIGH |
| tests after approval | [FRAME] 添加五类结构/descriptor hash、非法 source_ref、view ID golden、任何 semantic 输入改变 hash、同源不同 clock 的 view hash/state hash/view ID 稳定、及 built_at 不改变 CAS state 的测试。 CONFIDENCE: HIGH |
### D06_VALIDATION_FALLBACK

| field | contents |
|---|---|
| evidence | [KNOWN] Ledger 已列出 validation failed/fallback event types；现有 `RetrievalService.retrieve` 是真实读取入口，且具 capability used/denied audit 契约。 CONFIDENCE: HIGH |
| recommended resolution | [FRAME] `RetrievalService.retrieve` 通过内部 `VerifiedDerivedViewReader` 读取派生值；该 reader 在使用前重算 root、验证完整 I/L/B/V scope、五类完整性、builder、content/view hash 与 guard state。读取验证失败只转为权威候选读取，不写 derived validation/fallback event；既有 capability used/denied audit 仍按原契约决定。重建 transaction 中候选验证失败先 append F=`derived_view_validation_failed` 再 append G=`derived_view_fallback`，不改派生表，返回 `DerivedViewFallback(status="fallback",identity_id=I,lineage_id=L,branch_id=B,vault_id=V,authoritative_read_required=true,...,validation_failure_event_id=F,fallback_event_id=G)`；成功 CAS replace 后只 append R，并返回 `RebuiltMaterializedViews(status="rebuilt",identity_id=I,lineage_id=L,branch_id=B,vault_id=V,manifests=<ordered-five>,...)`。首次结果和 receipt replay 都必须显式从 value dict rehydrate 为这两个 Pydantic 类型之一，并重算 root 以闭合 aggregate lineage。 CONFIDENCE: HIGH |
| rejected alternatives | [INFERRED] 拒绝把读取校验失败写入 Ledger、以空 value 表示 fallback、不 rehydrate replay、让 fallback 使用五个 manifest，或直接信任 aggregate lineage 而不重算 root；这些方案会污染读取、使调用方不能判别状态、失去类型保证、伪造成功产物或绕过 lineage 绑定。 CONFIDENCE: HIGH |
| interface impact | [FRAME] 需后续 Sol 批准：P3 添加内部 reader，修改 retrieval service/test 以接入，并使 fallback/success 聚合结果携带非权威 I/L/B/V；不新增 ViewBuilder 反写权威入口。 CONFIDENCE: HIGH |
| tests after approval | [FRAME] 添加 reader 成功/失败、读取失败零 ViewBuilder 审计而原 capability audit 保持、success/F-G 顺序、rollback、首次/replay rehydrate、fallback I/L/B/V shape、伪造 aggregate lineage 的 root 重算拒绝测试。 CONFIDENCE: HIGH |
### D07_AUDIT_PAYLOADS

| field | contents |
|---|---|
| evidence | [KNOWN] LedgerEvent 绑定 command/hash/correlation/previous hash，且 inline payload 为闭合 canonical JSON。 CONFIDENCE: HIGH |
| recommended resolution | [FRAME] append 前查询 `prior_derived_event_id`：I/L/B/V 精确匹配、event_type 属于三种派生事件、`ledger_seq<next_ledger_seq`，取最大 seq；F/G 共享同一 pre-command prior，G 另以 `validation_failure_event_id=F` 直接链接。成功 R payload 精确键集为 `{operation,scope_refs,source_watermark_seq,source_root_hash,builder_version,before_derived_state_hash,after_derived_state_hash,view_ids,manifest_hashes,content_hashes,prior_derived_event_id}`；F 为 `{operation,scope_refs,source_watermark_seq,source_root_hash,builder_version,validation_stage,validation_code,before_derived_state_hash,after_derived_state_hash,candidate_derived_state_hash,prior_derived_event_id}`；G 为 `{operation,scope_refs,source_watermark_seq,source_root_hash,builder_version,fallback_mode,before_derived_state_hash,after_derived_state_hash,validation_failure_event_id,prior_derived_event_id}`。scope 与 D02 相同；maps 都按五类顺序，`manifest_hashes=SHA256(canonical full manifest JSON)`，content hashes 为 canonical content hash；before/after/candidate 均用 D05 semantic projection，故不含 built_at。 CONFIDENCE: HIGH |
| rejected alternatives | [INFERRED] 拒绝 append 后才查询 prior、跨 scope prior、F/G 各自重新查询 prior、省略 content hashes、用 built_at 参与 state hash 或自由 payload；这些方案会造成错误因果、跨 Vault 关联、时间非确定性或不可验证替换。 CONFIDENCE: HIGH |
| interface impact | [FRAME] 需后续 Sol 批准：既有 event type 枚举不新增值；P3 仅为三类既有事件加入 payload validator 与 prior query。 CONFIDENCE: HIGH |
| tests after approval | [FRAME] 添加三个精确键集、prior scope/seq、F-G linkage、manifest/content hash golden、同源不同 clock state hash 和 inline canonical payload 的测试。 CONFIDENCE: HIGH |
### D08_AC032_REJECTION_ENTRY

| field | contents |
|---|---|
| evidence | [KNOWN] 真实现有权威写入边界是 `AuthorityRepository.save_authoritative(schema_root, body)`；当前没有 ViewBuilder runtime API 或隐藏的视图写回入口。 CONFIDENCE: HIGH |
| recommended resolution | [FRAME] AC-032 special-case 必须位于 `AuthorityRepository.save_authoritative` 方法入口，在任何 `validate_authoritative_record`、`_store_authority` 或 `_store_projection` 前：当 schema root 是 `materialized_view_manifest` 或 body 表示 MaterializedViewManifest 时，直接抛出 `CoreContractViolation(CoreErrorCode.MATERIALIZED_VIEW_NOT_AUTHORITY)`。直接 API 和任何 adapter 均覆盖同一边界并映射 `CORE-E-MATERIALIZED-VIEW-NOT-AUTHORITY`；不得开启 UoW。因此 authority、Ledger、receipt、derived table 均零 mutation。 CONFIDENCE: HIGH |
| rejected alternatives | [INFERRED] 拒绝 `overwrite_memory_from_view`、将 generic schema mismatch 冒充 AC-032、在 validate/store/projection 后才 special-case、或为拒绝 append audit；这些方案会创设危险入口、错误映射、留下部分写入可能或伪装已执行命令。 CONFIDENCE: HIGH |
| interface impact | [FRAME] 需后续 Sol 批准：只在既有 repository 入口增加受限 pre-validation special-case；不新增由 ViewBuilder 写回 Memory 的 API。 CONFIDENCE: HIGH |
| tests after approval | [FRAME] 添加直接 API 和 adapter AC-032 测试，断言 MATERIALIZED_VIEW_NOT_AUTHORITY 且 authority/ledger/receipt/derived 四类计数均不变；完整 B02 reviewed conversion 前不得声称 canonical AC-032 acceptance。 CONFIDENCE: HIGH |
### D09_REGISTRY_AND_EXPOSURE

| field | contents |
|---|---|
| evidence | [KNOWN] MaterializedViewManifest 已排除在 `AUTHORITATIVE_MODELS`/`TYPE_REGISTRY`；现有 `WRITE_API_REGISTRY` 是 operational registry，而实现计划提及的 public-signature registry 尚未落地。 CONFIDENCE: HIGH |
| recommended resolution | [FRAME] ViewBuilder、manifest、content、success/fallback 永不进入 `AUTHORITATIVE_MODELS` 或 `TYPE_REGISTRY`。现有 `WRITE_API_REGISTRY` 必须另加 operational spec `rebuild_materialized_views`：target type 仅 `LedgerEvent`，actor `maintainer`，emitted types 精确为 rebuilt、validation failed、fallback 三类既有事件；派生对象永不为 write target。public-signature registry 是另一冻结面，不能混同。P1 allowed writes 为 `contracts/views.py`、`contracts/__init__.py`、`contracts/write_api_registry_v0_1.py`、`storage/repository.py`、`tests/contracts/test_views.py`、`tests/contracts/test_authority_write_surface.py`（含 D08 repository contract）；P2 为 `storage/migrations/0002_derived_views.sql`、`storage/database.py`、`storage/derived_views.py`、`storage/__init__.py`、`tests/storage/test_database.py`、`tests/storage/test_derived_views.py`；P3 为 `retrieval/view_builder.py`、`retrieval/__init__.py`、`retrieval/service.py`、`storage/unit_of_work.py`、`tests/retrieval/test_view_builder.py`、`tests/retrieval/test_retrieval.py`、`tests/storage/test_unit_of_work.py`，其中 verifier protocol 位于 view_builder，UoW 和其测试仅因 CommandAbortWithoutReceipt 证据而列入；P4 是完整 B02。 CONFIDENCE: HIGH |
| rejected alternatives | [INFERRED] 拒绝登记派生类型为 authority、P1 暴露未实现 runtime、混同两种 registry、无证据列 repository/UoW、漏列无 receipt abort 测试、或把 AC-031/032 手写测试叫 B02；这些方案会创建第四权威层、暴露空表面、误改冻结面、扩大范围、遗漏异常滥用边界或伪造 canonical acceptance。 CONFIDENCE: HIGH |
| interface impact | [FRAME] 需后续 Sol 批准：P1→P2→P3→P4 是依赖而非授权。P4 必须覆盖 ordinals 21–40、AC-019#1 至 AC-037#1、20 reviewed case paths、batch test、Data commit→Audit commit、B02.json、audit test 和 author/reviewer separation；任何 commit 仍需新的用户/Sol 授权，本提案不授予。仅 AC-031/032 手写测试是 noncanonical。 CONFIDENCE: HIGH |
| tests after approval | [FRAME] P1 验证非权威注册、operational spec、D08；P2 验证 migration/store/CAS；P3 验证 verifier、CommandAbortWithoutReceipt 的空/非空 event IDs、command、reader、rehydrate、fallback/audit；P4 仅在完整 B02 review/verification 后作 canonical acceptance。fixture 缺口不单独阻断获批后 P1–P3 低层 TDD，但阻断 canonical AC claim。 CONFIDENCE: HIGH |

### Complete before/after interface diff

[KNOWN] Before（冻结基线）：

```python
ViewBuilder.rebuild(command, vault_id, branch_id) -> CommandResult[MaterializedViewManifest]
```

CONFIDENCE: HIGH

[FRAME] After（需后续 Sol 批准）：

```python
class CommandAbortWithoutReceipt(Exception):
    # accepts only CommandResult(event_ids=())
    result: CommandResult[object]

class MaintenanceAttestationVerifier(Protocol):
    def verify(self, attestation: str, payload_hash: str) -> bool: ...

class MaintenanceApprovalRegistry(Protocol):
    def is_approved(self, approval_refs, evidence_seal_ref, identity_id,
                    lineage_id, branch_id, exact_operation,
                    exact_resource_ref) -> bool: ...

class RebuiltMaterializedViews(FrozenModel):
    status: Literal["rebuilt"]
    identity_id: RecordId
    lineage_id: RecordId
    branch_id: RecordId
    vault_id: RecordId
    manifests: tuple[MaterializedViewManifest, ...]
    source_watermark_seq: int
    source_root_hash: HashHex
    # validator: exact ordered five; manifest I/B/V and watermark/root/builder agree.
    # lineage closes via command/capability/vault + root/hash/store/reader, not manifest field.

class DerivedViewFallback(FrozenModel):
    status: Literal["fallback"]
    identity_id: RecordId
    lineage_id: RecordId
    branch_id: RecordId
    vault_id: RecordId
    authoritative_read_required: Literal[True]
    source_watermark_seq: int
    source_root_hash: HashHex
    validation_failure_event_id: RecordId
    fallback_event_id: RecordId

ViewBuilder.rebuild(command: MutationCommandEnvelope) \
    -> CommandResult[RebuiltMaterializedViews | DerivedViewFallback]
```

[FRAME] 命令 payload 增加 generation/state hash；target refs 保持 capability+R/F/G 的权威允许写入超集，不把 nonauthority guard 加入 expected versions。两 verifier 结果均以 `is True` 判定；precondition/CAS abort 无 receipt，首次/replay success 或 fallback 均显式 rehydrate union value 并重算 root。
CONFIDENCE: HIGH
### Follow-on packet order and dependency boundary

[FRAME] 顺序为 P1 contract/registry/repository boundary，P2 migration/store/CAS（依赖 P1），P3 runtime/reader/UoW TDD（依赖 P1/P2），P4 完整 20-case B02 conversion/review（依赖 P3）。P4 前 canonical AC-031/032 不可用；P1–P3 低层 TDD 可在规格获批后独立执行。
CONFIDENCE: HIGH

[FRAME] P4 的 Data commit→Audit commit、B02.json、batch/audit evidence 和 author/reviewer separation 仍需要新的用户/Sol commit authority；上述依赖不是本包的执行许可。
CONFIDENCE: HIGH

[KNOWN] remaining RED exact: `ViewBuilder implementation absent; AC-031/032 reviewed fixtures absent; Sol proposal approval pending`
CONFIDENCE: HIGH

[UNKNOWN] 尚未有 P1/P2/P3 实现、MaintenanceAttestationVerifier 或 MaintenanceApprovalRegistry 的运行证据、CommandAbortWithoutReceipt 行为证据、three-table migration/store 证据、完整 B02 review、AC-031/032 canonical acceptance 或后续 migration/runtime 测试结果。
CONFIDENCE: HIGH
## Effective only after Sol approval

[FRAME] 仅在 Sol 明确批准本提案后，P1 才可开始评审；P2、P3、P4 仍分别需要其前置证据和单独批准。未批准前，冻结 `ViewBuilder.rebuild` 基线、所有权威层、Ledger append-only、Governor 正常 Memory sole-writer、派生视图不得反写权威层的规则均保持不变。
CONFIDENCE: HIGH

[KNOWN] Routing ledger：`dispatch_id=m52a3-spec-terra-001`；`task=/root/terra_m52a3_spec`；`model=gpt-5.6-terra`；`reasoning_effort=xhigh`；`fork_turns=none`；`role=Terra sole writer`；`allowed_writes=[active/outputs/M5.2a3-ViewBuilder-契约闭合提案-2026-08-06.md]`；`packet_id=M5.2a3-viewbuilder-spec-closure-20260806-r1`；`spawn=/root`；`revision=m52a3-spec-terra-001-f3`；`scope_deviation=none`；`commit=false`；`push=false`；`release=false`。
CONFIDENCE: HIGH

[FRAME] [我打破的规则 / RULES I BROKE]：none。
CONFIDENCE: HIGH
