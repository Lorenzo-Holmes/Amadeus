STATUS=PROPOSED_AMENDMENT
PARENT_SHA256=D2AC0D8B23544141E38C90A69C6AD0A9A0AD96A245077E6CDD1794A3F7232893
PROPOSED_SUPERSESSION=D02,D04,D05,D06,D07_RUNTIME_AMBIGUITIES_ONLY
IMPLEMENTATION_AUTHORITY=NONE
INTELLIGENCE_AXIS=NONE
SOL_GATE=PENDING
GIT_AUTHORITY=NONE
P3_RUNTIME_STATUS=NOT_AUTHORIZED

# M5.2a3 P3 Runtime Contract Amendment — 2026-08-06

[INFERRED] 最强反方论据：若沿用父提案中尚未冻结的 D02、D04、D05、D06、D07 运行时细节直接实现 P3，派生索引会在同一输入下产生不可审计的 root/hash、错误地消费 capability，或在 reader 失效时污染 Ledger；因此任何 runtime 或测试实现都不应从该歧义获得权限。CONFIDENCE: HIGH

[FRAME] 本文是只针对 `D02,D04,D05,D06,D07_RUNTIME_AMBIGUITIES_ONLY` 的拟议补充；它等待 Luna 证据映射与 Sol 门禁，既不是实现许可，也不是测试、提交、推送或发布的事实。`P3_RUNTIME_STATUS=NOT_AUTHORIZED` 保持为本补充的机械状态。CONFIDENCE: HIGH

## 0. 冻结输入、边界与符号

[KNOWN] 本 TaskPacket 的写前快照为 branch=`codex/stage0c-fixture-conversion`、HEAD=`85f9d3ed2c8e37eb68a4cdfb6e9d11b56cd69e2e`、dirty entries=`30`、目标文件缺失；父文件为 `outputs/M5.2a3-ViewBuilder-契约闭合提案-2026-08-06.md`，其 SHA-256 与本文件 header 的 `PARENT_SHA256` 相同。CONFIDENCE: HIGH

[KNOWN] Python canonical manifest 为 `168 / A1A10F799CBB06C0F0F50D22FE1E3E86770DA075486C573B9361C5D644EBA89B`。父文档机械 header 仍是 DRAFT/PENDING；此前门禁已批准其 P1/P2 路径，`P2_GATE=PASS`，而 P3 runtime 仍未获授权。计划、文档、代码、测试、commit 与 push 是不同状态，本文不将它们合并为同一结论。CONFIDENCE: HIGH

[FRAME] 下文中 `I,L,B,V,W,T,ROOT,CONTENT,DESCRIPTORS` 是契约符号：`I=identity_id`、`L=lineage_id`、`B=branch_id`、`V=vault_id`、`W=source_watermark_seq`、`T=view_type`。符号等式只定义拟议运行时行为，不报告现实运行结果。CONFIDENCE: HIGH

## 1. 冻结决定 1 — descriptor 集合与禁止字段（D04）

[FRAME] descriptor 必须逐字为：snapshot=`{"layer":"source_snapshot","record_id":snapshot_id,"record_version":version,"integrity_hash":record_header.content_hash,"payload_root_hash":payload_root_hash}`；ledger=`{"layer":"ledger_event","record_id":event_id,"record_version":version,"integrity_hash":record_header.content_hash,"ledger_seq":ledger_seq}`；memory=`{"layer":"autobiographical_memory","record_id":memory_id,"record_version":version,"integrity_hash":record_header.content_hash,"semantic_version":semantic_version}`。CONFIDENCE: HIGH

[FRAME] Ledger descriptor 只来自完整 verified replay，且 `content_hash==event_hash`。descriptor 中禁止 `payload`、`time`、`source_ref`、`event_type` 与任何 Memory state。CONFIDENCE: HIGH

## 2. 冻结决定 2 — source-root preimage、顺序与 watermark（D04）

[FRAME] source-root preimage 必须逐字为 `{"source_root_version":"derived-view-source-root-v1","scope_refs":{"identity_id":I,"lineage_id":L,"branch_id":B,"vault_id":V},"source_watermark_seq":W,"descriptors":DESCRIPTORS}`；hash=`sha256_hex(canonical_json(preimage))`。CONFIDENCE: HIGH

[FRAME] 排序精确为 active snapshots `(snapshot_id,version)` → 排除 R/F/G 的 ledger `(ledger_seq,event_id)` → 全部 scope memory `(memory_id,semantic_version,version)`；`W` 为纳入 Vault ledger 的最大 seq，或在无纳入 ledger 时为 `0`。CONFIDENCE: HIGH

## 3. 冻结决定 3 — view-hash preimage 与排除项（D05）

[FRAME] view-hash preimage 必须逐字为 `{"view_hash_version":"materialized-view-hash-v1","view_type":T,"scope_refs":{"identity_id":I,"lineage_id":L,"branch_id":B,"vault_id":V},"source_watermark_seq":W,"source_root_hash":ROOT,"builder_version":"view-builder-v1","content":CONTENT}`。CONFIDENCE: HIGH

[FRAME] view hash 的输入排除 `built_at`、`generation`、`view_id` 与 `manifest_hash`。CONFIDENCE: HIGH

## 4. 冻结决定 4 — D07 audit payload、JSON shape、顺序与链路（D07）

[FRAME] 三类事件的 `operation` 必须是 `rebuild_index`；F 的 `validation_stage` 必须是 `candidate_prewrite`，`validation_code` 必须是 `candidate_rejected`；G 的 `fallback_mode` 必须是 `authoritative_read`；`scope_refs` 必须是 JSON array `[I,L,B,V]`。CONFIDENCE: HIGH

[FRAME] `view_ids`、`manifest_hashes`、`content_hashes` 都必须是 JSON object，键集精确为五个 view types：`summary`、`timeline`、`vector`、`fulltext`、`cue`。canonical JSON 决定字节顺序，绝不依赖 insertion order。R 的 before=`current`、after=`candidate`；F/G 的 before=after=`current`；F 的 candidate=`candidate`；F/G 共用同一 pre-command prior，G 的 `validation_failure_event_id=F`。CONFIDENCE: HIGH

[FRAME] R payload exact key set 为 `{operation,scope_refs,source_watermark_seq,source_root_hash,builder_version,before_derived_state_hash,after_derived_state_hash,view_ids,manifest_hashes,content_hashes,prior_derived_event_id}`。F payload exact key set 为 `{operation,scope_refs,source_watermark_seq,source_root_hash,builder_version,validation_stage,validation_code,before_derived_state_hash,after_derived_state_hash,candidate_derived_state_hash,prior_derived_event_id}`。G payload exact key set 为 `{operation,scope_refs,source_watermark_seq,source_root_hash,builder_version,fallback_mode,before_derived_state_hash,after_derived_state_hash,validation_failure_event_id,prior_derived_event_id}`。三个 payload 均禁止 extra key。CONFIDENCE: HIGH

## 5. 冻结决定 5 — capability 与 approval 的精确 Vault 绑定（D02）

[FRAME] `capability.exact_resource_ref`、approval registry 的参数、以及从命令推导出的资源，三者必须逐字等于 `V`。CONFIDENCE: HIGH

## 6. 冻结决定 6 — CommandEnvelope、错误分类与无 receipt abort（D02）

[FRAME] 非精确的 `MutationCommandEnvelope` 输入必须在任何 DB access 前抛出 `TypeError`。command type、actor、payload 的 keys+types、scope、operation、builder、event-ID semantics 的错误必须归类为 `HEADER_BODY_MISMATCH`；target、expected-version 的 set/order、或 declared value 的错误必须归类为 `VERSION_TARGET_SET_MISMATCH`；authority current version 或 derived guard 的错误必须归类为 `STALE_VERSION`。CONFIDENCE: HIGH

[FRAME] `CommandAbortWithoutReceipt` 只接受 `value=None,event_ids=(),error!=None,replayed=False`。错误的 type 或 value 使用分别抛出 `TypeError` 或 `ValueError`；UoW 在 transaction 外捕获该 abort，且不写 receipt。CONFIDENCE: HIGH

## 7. 冻结决定 7 — capability projection、consume 与并发裁决（D02）

[FRAME] handler 在 capability body 后读取 projection，并核对 type、identity、branch、status、expiry 与 version。`remaining_uses` 只允许 built-in `0` 或 `1`：`0` → `CONSUMED`，`1` 才继续；其他值或静态字段错配是 integrity failure。CONFIDENCE: HIGH

[FRAME] 并发的最终裁决仍是 `repository.consume_capability`；条件更新失败 → `CONSUMED` 且 no-receipt。禁止以 `save_authoritative` 修改 capability。CONFIDENCE: HIGH

## 8. 冻结决定 8 — candidate validator protocol 与严格返回值（D06）

[FRAME] protocol 必须逐字为：`DerivedViewCandidateValidator.validate(*, scope: DerivedViewScope, entries: tuple[DerivedViewEntry,...], source_watermark_seq:int, source_root_hash:str, candidate_semantic_state_hash:str)->bool`。CONFIDENCE: HIGH

[FRAME] 只有 built-in `True` 是 success；只有 built-in `False` 进入 F/G。异常或任何其他返回类型均为 validator malfunction，整个 transaction rollback、零 receipt、零 audit；validator 必须在任何 derived DML 前调用。CONFIDENCE: HIGH

## 9. 冻结决定 9 — 首次执行与 receipt replay 的 union rehydrate（D06）

[FRAME] 首次执行必须在 transaction 内重算 current root。receipt replay 必须按 status 显式 rehydrate 为 `RebuiltMaterializedViews|DerivedViewFallback`，并核对 command scope、event IDs 与 immutable R 或 F/G ledger payload。CONFIDENCE: HIGH

[FRAME] replay 不与 current authority root 比较；历史 audit-bound root 为准。replay 不得新增 fallback/audit，也不得改写 receipt。CONFIDENCE: HIGH

## 10. 冻结决定 10 — VerifiedDerivedViewReader 命中、miss 与 authority 传播（D06）

[FRAME] Reader 在 P2 store 的 canonical/five/guard 检查之后，重算 current root+watermark，验证精确 builder/content/view hash/semantic state；missing、stale、derived-only corruption 一律为 miss。authority/Vault/Ledger integrity failure 必须传播。CONFIDENCE: HIGH

[FRAME] 命中仍调用 `validated_vault_candidates`；fulltext refs 只作交集与排序，并覆盖每个权威 candidate 恰好一次。miss 走原权威路径。两条路径维持原 capability audit 一次；读失败产生零 R/F/G。CONFIDENCE: HIGH

## 11. 冻结决定 11 — public surface、constructor 与 export（D06）

[FRAME] constructors/public surface 必须精确为：`ViewBuilder(connection, attestation_verifier, approval_registry, clock, *, candidate_validator=None)`；`ViewBuilder.rebuild(command: MutationCommandEnvelope)->CommandResult[RebuiltMaterializedViews|DerivedViewFallback]`；`VerifiedDerivedViewReader(connection)`。CONFIDENCE: HIGH

[FRAME] `RetrievalService` 的 constructor 与 `retrieve` signature 完全不变，reader 在其内部构造。package `__all__` 只新增 `ViewBuilder`；reader 及三个 protocols 不作 package re-export。CONFIDENCE: HIGH

## 12. 冻结决定 12 — 合法空 scope、head 与 prior null（D04/D07）

[FRAME] 合法空 scope 使用 `DESCRIPTORS=(),W=0`、五个空索引。append 前仍必须有 verified branch ledger head；无 head 是 integrity error 并 rollback；无 prior 时 payload 保留 `null`。CONFIDENCE: HIGH

## 13. 冻结决定 13 — 精确执行序、DML 与回滚（D02/D04/D06/D07）

[FRAME] 精确顺序为：public structural validation；UoW receipt replay → authority target-version guard；handler capability body/projection → attestation → approval → Vault → derived guard → single verified ledger replay → source/head/prior snapshot → build → candidate validate。CONFIDENCE: HIGH

[FRAME] Success 必须为：`replace_scope → consume → append R → return(R,) → receipt → commit`。Fallback 必须为：`zero derived DML → consume → append F → append G → return(F,G) → receipt → commit`。任何后续失败必须 full rollback；unused event IDs 保持 absent。CONFIDENCE: HIGH

## Before/after contract diff

[FRAME] 父提案 D04 缺少 ledger/memory descriptor 与 source-root 的 exact literal；本补充在决定 1–2 冻结二者。CONFIDENCE: HIGH

[FRAME] 父提案 D05 缺少 view-hash 的 exact literal 与排除 key；本补充在决定 3 冻结二者。CONFIDENCE: HIGH

[FRAME] 父提案 D07 对 maps、order 与 literals 存在运行时歧义；本补充在决定 4 与 12–13 冻结 JSON object key set、canonical-byte ordering、prior、R/F/G shape 和 append 顺序。CONFIDENCE: HIGH

[FRAME] 父提案 D02 缺少 consume、error 分类与 no-receipt abort 的详细可执行契约；本补充在决定 5–7 与 13 冻结这些边界。CONFIDENCE: HIGH

[FRAME] 父提案 D06 缺少 replay、reader 与 constructors 的精确契约；本补充在决定 8–11 冻结 strict validator、union rehydrate、reader 行为、surface 与 export。CONFIDENCE: HIGH

[FRAME] 该 diff 仅是 proposed amendment，等待 Luna/Sol；它不把 P1/P2 的已完成门禁事实转换为 P3 runtime authority。CONFIDENCE: HIGH

## 既有 P1/P2 门禁证据（不在本 turn 重跑）

[KNOWN] P1 final file hashes：`views.py=BEA400B4335BC5581D0CC274266CA84C3D8A9847A4EA6B4840FE6AC0F75AABE1`；`contracts/__init__.py=2BAB5621D204E0419AB85C9796200C7AB78583B9065879893F1C6F266C97865D`；`write_api_registry=46D7F0AA7EC3501B1BD98698183A15F262C041D074EB52D5AB84574116374D73`；`repository.py=F280AF71CCA1B341EC271B5B6BDFB2B9D287EB4D664FF5F07819D9A810C0E941`；`test_views=0681FD5B63BFB8E34615A03329ABDAE8D21A59387D46398315109711E15A6AB6`；`test_authority_write_surface=3AFF771EC4EE2BA627D8C69479366F604E342EFE46A64871F71FA117B16A5C03`。`P1_GATE=PASS` 是此前门禁状态；P1 exact test counts 未提供，不能编造。CONFIDENCE: HIGH

[KNOWN] P2 final file hashes：`0001=5DAAB4E63E205386284EFD10319727BA422EF19DA10BA3472D87ADF2AB795ACB`；`0002=67F30B87C8FFD06CDB36622A7AE6DE16DA5B3B814E4BDD7CABEE3D183C4971F8`；`database=8DE59F7F967828DB08E6BB5BD7B3C9631F428B52797987B18C811B150D02084B`；`derived_views=AE1B038D67AAE46DC6BB5D1C4411AB44A8E4BE388D9A260BAAC9AE210B83A631`；`storage/__init__=9A4A57C112EC89A17B0EF6D331D3C5E0AB04FBA448D75004259E2DBCE8B1F996`；`test_database=B444FEFCC4D3287C6D4D2DFADF303979D7696A04CAEF873B2CE7C6F969DCA126`；`test_derived_views=70ADCA707D6105ACFAE21F2BB50B3604BA492E96251D9A82C2A10AC59C2AEE41`；`test_idempotency=63D128B24CFD27990F4133F5151797F93D53D2614EC1A8EF45B7B0E17FA4E24B`。CONFIDENCE: HIGH

[KNOWN] P2 r1c fixture hashes：`tests/governance/test_governor_decision_signature.py=89A991E5686C068A104597FE75035B511059BE1B74C5CDC6584EAA997348B430`；`tests/governance/test_receipt_authority.py=67AE63144B7F7E159BED6BCA1C9497A1114EFB9447B140C84D62E5F8E7BD250A`；`tests/governance/test_request_guards.py=AF708321CF2938C29AD23EB09636FC540A4EBA992537892F8F518985C5380306`；`tests/governance/test_governor_receipt_ancestry.py=6AD1B397B4D61CFCCFD017BB9D6E1F5EF6E6174241341D77713D2F8083F48593`；`tests/retrieval/test_expression.py=CE937285A337F609805DA819BBDE6474E482164551220DA23DAF5EB7282832BD`。r1b `tests/storage/test_idempotency.py=63D128B24CFD27990F4133F5151797F93D53D2614EC1A8EF45B7B0E17FA4E24B`。这些是此前门禁证据，不是本 turn 重跑。CONFIDENCE: HIGH

[KNOWN] `P2_GATE=PASS` 的 prior closure provenance 为 owning=`104`、storage=`238`、contracts=`121`、governance=`178`、retrieval=`166`、B01=`18`、invariants=`46`，均为此前门禁的 exit `0`；本 turn 不重跑它们。CONFIDENCE: HIGH

## 后续 P3 测试矩阵（提案，不是执行授权）

[FRAME] 决定 1：测试 descriptor golden bytes、三层包含/排除、verified replay 及禁止字段。决定 2：测试 root preimage golden bytes/hashes、排序与 watermark。决定 3：测试 view-hash preimage、排除项与 deterministic hash。CONFIDENCE: HIGH

[FRAME] 决定 4：测试 R/F/G exact audit key set、五 view maps、canonical-byte order、prior/linkage。决定 5：测试 exact `V` resource binding 与 approval 参数。决定 6：测试 command/error 分类、`CommandAbortWithoutReceipt` 和 zero receipt。CONFIDENCE: HIGH

[FRAME] 决定 7：测试 projection consume、`remaining_uses` strictness、repository CAS 与 no-receipt。决定 8：测试 validator built-in bool type strictness、malfunction rollback。决定 9：测试 first/replay union rehydrate、immutable audit payload 与 historical-root 语义。CONFIDENCE: HIGH

[FRAME] 决定 10：测试 reader hit/miss/corruption、authority propagation、fulltext candidate coverage 与 capability audit count。决定 11：测试 constructor/signature/export。决定 12：测试 empty scope、ledger head、prior null。决定 13：测试 exact order、R vs F/G、CAS/rollback、unused event-ID absence。CONFIDENCE: HIGH

## RED、UNKNOWN 与门禁状态

[KNOWN] remaining RED：P3 runtime/tests absent；AC-031/032 reviewed B02 fixtures absent；addendum pending Luna/Sol。CONFIDENCE: HIGH

[UNKNOWN] repository-wide full suite 未运行；本 TaskPacket 禁止本 turn 运行 pytest，因此不存在新的 suite 结果。CONFIDENCE: HIGH

[UNKNOWN] P1 exact test counts 未提供；本文不推断或编造计数。CONFIDENCE: HIGH

[FRAME] intelligence axis=`NONE`；本补充不声明任何可观察能力提升。CONFIDENCE: HIGH

## Routing ledger

[KNOWN] `dispatch_id=M5.2a3-P3-RUNTIME-CONTRACT-AMENDMENT-20260807-01`；`task_name=/root/terra_p3_contract_amendment`；`role=Terra sole writer`；`allowed_writes=[active/outputs/M5.2a3-P3-runtime-contract-amendment-2026-08-06.md]`；`packet_id=M5.2a3-P3-RUNTIME-CONTRACT-AMENDMENT-20260807-01`；`spawn_record=/root`；`agent_id=/root/terra_p3_contract_amendment`；`git_authority=NONE`；`scope_deviations=none`。CONFIDENCE: HIGH

[UNKNOWN] 当前 dispatch 的 model、reasoning effort 与 fork_turns 未在本 TaskPacket 中提供；本文不捏造它们。CONFIDENCE: HIGH

[KNOWN] completion status 仅为 `PROPOSED_AMENDMENT_DOCUMENT_CREATED`；它不表示 P3 runtime、P3 tests、commit、push 或 Sol approval 已完成。CONFIDENCE: HIGH

## 规则审计

[FRAME] [我打破的规则 / RULES I BROKE]：none。本文只创建该拟议补充文档；未声称 runtime authority，未把此前门禁转写为本 turn 测试或实现结果。CONFIDENCE: HIGH
