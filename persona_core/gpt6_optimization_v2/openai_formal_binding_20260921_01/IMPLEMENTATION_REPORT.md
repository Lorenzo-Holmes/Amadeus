# Frozen OpenAI formal successor binding

Implementation and offline verification are complete. The exact-model input framing proof is not available, so the formal preflight is NOT_READY and no Provider request or fresh validation revision was created. This is an engineering checkpoint; it does not establish conversation quality or state-integrity acceptance.

Project-completion spend authorization is recorded. Historical budget decisions remain immutable and are superseded only for future execution. The current blocker is INPUT_BOUND_PROOF_NOT_READY.

## Implementation

The formal scope-7 branch binds the frozen model schedule, closed Responses payload, source and policy identities, direct route, stream terminal classification, raw/native receipts and usage reservations. Scope-6 retains its original synthetic registry. No retry, fallback, count call, hidden-reasoning promotion or output-token double billing is introduced.
Existing bounded display, admission, strict proof, dual-gate consumer and original 44/176 evaluation semantics are unchanged. Each future paid turn requires review of the preceding captured turn before the next submission. A candidate-wide allocation marker permits only one fresh revision.

Eight exact paths: the original seven plus the documented transcript_store.py extension. The extension permits only the isolated work/openai_formal_binding sandbox parent; it changes no data schema or admission semantics.

- ADD `persona_core/operational_runtime_v1/provider_openai.py`
- MODIFY `persona_core/operational_runtime_v1/provider_adapters.py`
- MODIFY `persona_core/operational_runtime_v1/provider_contract.py`
- MODIFY `persona_core/operational_runtime_v1/provider.py`
- MODIFY `persona_core/gpt6_optimization_v2/tools/evaluation_runner.py`
- MODIFY `persona_core/gpt6_optimization_v2/tools/formal_binding_preflight.py`
- ADD `persona_core/gpt6_optimization_v2/tools/test_openai_formal_binding.py`
- MODIFY `persona_core/operational_runtime_v1/transcript_store.py`

## Verification

New tests 40/40 PASS, covering all 30 frozen requirements. Component regression 821/821 PASS across 34 modules. Legacy 450 retains 449 compatible passes and one expected historical source-identity sentinel; its raw exit code remains 1 and is not represented as 450/450 PASS.
Initial failures are retained: the old registry membership regression was fixed within the allowlist; two existing loopback tests needed a loopback-only guard; a concurrently running suite affected the new test directory-invariance assertion, so the final new suite was run separately. No regression expectation or historical artifact was rewritten.
New source freeze `OPENAI_FORMAL_SUCCESSOR_BINDING_20260921_01` contains 184 scanned members. The archived 182-member P3 parent remains exact; protected historical files remain unchanged. P1, quality policy, successor contract and configuration identities are preserved.

## Execution limit

The 28672-input-token budget is a conditional design assumption. No audited exact-model tokenizer plus complete Responses role/message-framing proof was found in the inspected local and official sources. The production proof registry is empty and rejects generation before credentials, journal submission or network access. Mock proof calculators exist only inside authored offline tests.
The formal preflight prerequisite audit returned NOT_READY without executing the formal preflight. Separate offline diagnostics constructed the 44-slot schedule and 7 initial contexts with 0 calls; they do not certify the input bound. Official input counting is a remote API and was not called.
Future conditional 44-slot estimate: 85.46304 USD or 752.074752 CNY under FX<=8 and fees<=10%; no new amount approval is needed. Current task API usage estimate 0.00 CNY, billed amount UNKNOWN, billing_certified=false. All current successor counts are 0; 176 criteria remain unreviewed. Historical R16 remains FAIL/permanently quarantined with its Major open. G6-08/09 stay LOCKED and completion flags remain false.

Establish an auditable exact-model local input-token/framing proof. Preserve this freeze; if proof integration changes source, test and freeze a successor identity. Then run the real zero-provider formal binding preflight. Only after READY and all identities MATCH may the already-authorized sole fresh 44/176 revision be allocated. Refresh execution-day prices and record finite reservations without another budget approval. Until then: no Provider/count/readiness calls, no fresh revision, no historical replay, no G6-08/09.
