# G6-07R7: formal trusted acceptance integration

Status: FROZEN_OFFLINE_INTEGRATION_PASS. Binding preflight: READY.

The formal external44 entry previously created OFF sessions despite a working
standalone acceptance layer. It now requires an explicit acceptance configuration
before allocating a paid revision. Mode, policy, source freeze, runtime, provider
configuration, dataset and rubric identities are recorded in the revision,
sessions and every accepted output. Ordinary sessions retain the OFF default.

The formal composition root installs the general frozen-catalog admission
adapter. A shared request builder sends the strict proposed-plan contract and
admitted inventory. Model certificates grant no authority. The host validates,
certifies and renders; raw provider bytes, raw assistant envelope, visible draft,
proposed plan, validation, certificates and accepted product text remain separate.

Display, history, next-turn context, memory observation, candidate input, blind
input and evaluation select accepted product semantics. Audit/review preserve
both raw and accepted evidence. The evaluator records raw violations, guard
action, transformation and containment independently of the product verdict.
Missing bindings or accepted persistence refuse execution; they never select raw
text as a fallback. Stop records distinguish all nine required failure layers.

## Offline evidence

| Check | Result |
| --- | --- |
| New focused integration checks | 45/45 PASS |
| Full component suite | 777/777 PASS; 32 modules |
| Retained prior baseline | All 31 prior modules byte unchanged; 732/732 PASS |
| Legacy regression | 43 modules / 450 checks; 449 compatible PASS, 1 expected historical source-identity sentinel, 0 unexpected failures |
| Formal driver with local synthetic transport | PASS, bounded mock turns; no target captures or semantic scores |
| Restart and next-turn propagation | PASS; accepted text and identity survive restart, raw strengthening never becomes context or memory |
| Post-freeze formal construction | TRUSTED_ACCEPTANCE_EXTERNAL44_BINDING_READY; 0 provider calls |
| Protected history | 536 protected files, 2133 historical evidence files, 25 journals, 7 UNKNOWN rows and spend ledger unchanged |

All tests use local fixtures under a non-loopback network guard. The post-freeze
preflight additionally forbids all socket connections and provider-call entry.
DeepSeek, OpenRouter, readiness and paid generation requests: 0. Automatic paid
retries: 0. No paid revision was allocated and no new 44/176 validation was run.
Provider/transport source, existing semantic rules and frozen Persona/Genesis
source remain unchanged.

## Scope of readiness

The production typed catalog contains no benchmark answers or per-turn semantic
annotations. Unrestricted input without independently admitted evidence remains
UNPARSED and receives the low-authority response. Synthetic catalogs prove the
binding and guard behavior on positive and adversarial structured cases; they
do not establish automatic interpretation of the evaluation questions, useful
general persona conversation, naturalness or a semantic validation PASS.

Freeze: TRUSTED_ACCEPTANCE_EXTERNAL44_INTEGRATION_20260920_01. Complete local manifest: 163 source
members. The public freeze binds only this publication's reviewed source, tests
and aggregate documents; private datasets, raw captures, databases, credentials,
hidden reasoning, evaluation evidence and spend ledger are excluded.

G6-07 remains VALIDATION_PENDING with FRESH_SEMANTIC_VALIDATION_REQUIRED.
The next fresh revision is READY_FOR_NEW_REVISION and remains uncreated, at
0/44 captures and 0/176 judgments. G6-08, G6-09, Candidate V2 and Blind remain
LOCKED. Historical semantic findings remain preserved and unclosed.
