# Reasoning scope representation — offline validation

The post-review repair adds explicit support for supplied premises, reported observations, inferred consequences, unresolved possibilities, conditional dependencies, and candidate-set completeness. Field comparison, sampling order, record order, causal order and pairing identity are distinct relation domains. Eliminating some candidates does not establish an exhaustive universe; a finite scope can be represented when its stated closure basis is retained among the premises.

Proposed interpretations remain structurally checked descriptions. They neither prove their premises nor validate semantic entailment, and they cannot authorize runtime events. The production context adds a sparse view of literal linguistic markers and quotation boundaries, preserving the full original utterance and its source. Markers do not classify the truth or meaning of a clause. Quoted speaker and addressee remain unresolved unless an explicit proposed interpretation supplies them. There is no automatic pronoun binding to the current assistant or user.

The actual ChatService request carries the new representation. Repeated speaker/authority metadata is grouped to retain the existing input budget. The ordinary-context and near-capacity regression limits remain unchanged. The blind-package consumer independently reconstructs the received context, supports the new projection version and rejects tampered reasoning metadata. It exposes neutral, source-bound context without developer scores or private identifiers.

Seventeen newly authored examples cover several scientific, records, correspondence and everyday settings, including English controls, negation and nested quotations. Twenty-two mechanism tests pass. All current G6 components pass: 331 tests across 21 modules. The complete unchanged legacy regression covers 43 modules and 450 tests: 449 compatible passes and one retained historical source-identity error. That error remains an error; historical source-bound semantic results cannot be inherited. No threshold was relaxed. Source/Genesis and heldout were not revised or used as repair feedback.

These results establish offline representation and data-flow behavior. They do not establish target-model semantic improvement, naturalness or field nonleakage. The observed semantic findings remain open until fresh complete validation. No acceptance score is assigned by these mechanism tests.

The transport implementation is unchanged. Existing complete streams reconstruct exactly; the interrupted stream still lacks terminal evidence and is refused as a formal reply. No observed local framing, buffer, IPC or deadline defect justifies a transport change. A generic public service-health status does not establish recovery of the interrupted request path. All four UNKNOWNs and the stopped revision remain quarantined. No real generation request or paid retry was made during this repair.

Current source is frozen for validation. Provider readiness remains blocked pending genuinely new recovery evidence. Fresh external validation, heldout, original-suite validation, candidate creation, independent review and new real dates must still satisfy their gates. Product acceptance and production activation remain false.
