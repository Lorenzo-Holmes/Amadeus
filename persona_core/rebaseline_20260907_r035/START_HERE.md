# R035 — Genesis Snapshot Freeze + Runtime Install

R035 以 R034 Freeze Package 为唯一安装输入。安装前逐文件验证冻结哈希，然后把冻结组件复制到 `persona_core/runtime/genesis/frozen/`，创建不可重复覆盖的 `INSTALL_LOCK.json`。

Genesis 只安装一个有范围来源回忆 `RC-R005-001`；Maho/Leskinen 作为 source relationship roots，不转成产品用户关系。产品关系初始为空，Affect 为 baseline deviation=0，身体/外部工具/环境控制/实体礼物交付均未授权。

Experience Ledger 的第0条仅为 GENESIS_EVENT，不反写 Source/Persona。
