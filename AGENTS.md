# Amadeus Persona Core — Active Codex Entry

Current execution governance: **APCORE_CODEX_GOVERNANCE_2_1**.

## Start and authority

Read persona_core/CODEX_EXECUTION_RULES.md, then the machine policy and pinned checkpoint returned by:

    python persona_core/gpt6_optimization_v2/tools/governance_v2.py resume

Stable architecture authority is persona_core/APCORE_CANONICAL_EXECUTION_GUIDE.md. Formal execution rules are persona_core/APCORE_CODEX_EXECUTION_POLICY_V2.md and persona_core/gpt6_optimization_v2/CODEX_EXECUTION_POLICY.json.

Latest explicit user requirements govern authorization. Code, original receipts/databases and pinned state establish facts; an old prompt cannot turn a failed or unknown observation into PASS.

The full previous AGENTS bytes and interrupted V2.0 draft are preserved in work/governance_v2_restart/restart_baseline. Old CURRENT blocks are historical checkpoints, not competing active instructions.

## Product scope and immutable principles

Default mainline: Amadeus Kurisu Persona Core, source-to-Genesis continuity, real text conversation, reliable storage and retrieval. Do not switch to UV, avatar, TTS, Unity or UI without a new user request.

Do not invent personality from impressions. Preserve traceable sources and distinguish Human Kurisu, encoded source memory, externally learned information, other continuities, and this runtime's own experiences.

Do not silently merge timelines/routes/media. Genesis experiences never rewrite Source Persona. Model output is not authority for facts, memory or identity. UNKNOWN stays uncertain; evidence-based corrections retain prior versions and reasons.

Conversation quality is required as well as state safety. Natural expression cannot excuse false memory or unauthorized state effects; refusing everything cannot substitute for a useful Persona Core.

DISPLAY_ELIGIBLE != FACT_TRUE != STATE_ADMITTED != STATE_COMMITTED.

44 original turns, 176 original criteria, Gate A 176, Gate B 132, original IDs/wording/order/rubric/routing, Major/Critical thresholds and leakage controls are frozen. Do not dilute or replace them.

## Execution and evidence

For proven local engineering defects: close the paid gate, preserve existing evidence, diagnose, repair, test and continue within the current task. Do not end a whole task merely because one local test failed.

Hard-stop paid execution for Critical, State Integrity Major, model/Conversation Utility blocking Major, Provider UNKNOWN/ambiguous terminal, benchmark contamination, unauthorized promotion, unrecoverable/unproven identity, or a genuinely unresolved research/product decision.

Never automatically retry a Provider request. Never erase failed revisions, UNKNOWNs, spend or unresolved quality observations. Preserve private/public separation. Current review is internal unless separately evidenced as independent.

R18 is immutable quarantined history with a proven evaluator defect and an unresolved MODEL_RAW/UNCLEAR observation. Its technical disposition is HARNESS_INVALIDATED_PERMANENT_HISTORY, not PASS. No R18 resume/replay/resend. No new paid validation in this governance task.

Verify once, seal and reuse until invalidated. Cache keys include source, transitive dependencies, tests, fixtures, configuration and environment. Hash-first means actual byte verification with compact output, not merely trusting a stored root string.

Use targeted and affected tests during repair. Run the full component baseline plus new required tests once per final source-freeze candidate. Reuse an identical valid receipt at preflight. Do not reinterpret expected legacy sentinels as zero raw failures.

## State and recovery

V2 canonical writer: persona_core/gpt6_optimization_v2/tools/governance_v2.py. Use immutable generation publication and SHA-256 CAS; never steal a lock or overwrite concurrent state blindly.

Old GOAL_STATE/TASK_GRAPH/RECOVERY_CURSOR/VALIDATION_MATRIX remain V1 scientific/task fact snapshots, not already-migrated V2 mirrors. Do not manually synchronize them for ordinary repair. Important new source/persona/architecture decisions still require the relevant decision log; this governance change does not unlock G6-08/09.

Read Master Goal, Progress, Continuation and Decision Log deeply only for a concrete identity mismatch, scope ambiguity, historical conflict or scientific/architecture decision. Preserve current authorization and checkpoint before stopping at a genuine boundary.

Commit once at a reviewed public-safe milestone. This is a partial work tree with a separate Git directory; stage exact allowed paths only. Never git add -A, reset, clean, or include unrelated missing files.
